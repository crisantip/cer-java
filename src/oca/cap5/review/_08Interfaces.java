/**
 * Elija la declaración correcta sobre el siguiente código: 
 * 
 * A. It compiles and runs without issue.
 * B. The code will not compile because of line 2.
 * C. The code will not compile because of line 3.
 * D. The code will not compile because of line 4.
 * E. The code will not compile because of lines 2 and 3.
 * (F). The code will not compile because of lines 3 and 4.
 * 
 * 
 * @author Katty
 * @since 30/01/2019
 */

package cap5.review;

public class _08Interfaces {

}

interface Herbivore {
	int amount = 10;

	//línea 3
	//public static void eatGrass();	//Debe tener un cuerpo

	//línea 4
	//public int chew() {	//Debe ser definido como static o default
	//	return 13;
	//}
}