package cap3.review;

import java.util.ArrayList;

/**
 * A. int length = l.capacity;
 * B. int length = l.capacity();
 * C. int length = l.length;
 * D. int length = l.length();
 * E. int length = l.size;
 * (F). int length = l.size();
 * G. None of the above.
 * 
 * Muestra los métodos o propiedades aplicadas a un ArrayList 
 * 
 * @author Katty
 * @since 11/12/2018
 */
public class _17ArrayListMethodsProperties {

	public static void main(String[] args) {

		/*
		ArrayList l = new ArrayList();
		
		int length1 = l.capacity;	//No existe
		int length2 = l.capacity();	//No existe
		int length3 = l.length;		//No existe
		int length4 = l.length();	//No existe
		int length5 = l.size;		//No es una propiedad
		int length6 = l.size();		//Es un método
		*/	
	}
}
